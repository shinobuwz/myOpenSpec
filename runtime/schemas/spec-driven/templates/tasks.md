## 1. <!-- 任务组名称 -->

- [ ] 1.1 [test-first] <!-- 任务描述 -->

  **需求追踪**：[R1] → [U1]
  **执行方式**：[test-first]
  **涉及文件**：
  - `src/...` — 实现
  - `test/...` — 测试

  **验收标准**：
  - [ ] <!-- 具体可验证的标准 -->

  **验证命令 / 方法**：
  - `<command>`，预期：<!-- 通过条件 -->

  **依赖**：无

- [ ] 1.2 [test-first] <!-- 任务描述 -->

  **需求追踪**：[R1] → [U1]
  **执行方式**：[test-first]
  **涉及文件**：
  - `src/...` — 实现
  - `test/...` — 测试

  **验收标准**：
  - [ ] <!-- 具体可验证的标准 -->

  **验证命令 / 方法**：
  - `<command>`，预期：<!-- 通过条件 -->

  **依赖**：Task 1.1

## 2. <!-- 任务组名称 -->

- [ ] 2.1 [characterization-first] <!-- 任务描述 -->

  **需求追踪**：[R2] → [U2]
  **执行方式**：[characterization-first]
  **涉及文件**：
  - `src/...` — 实现
  - `test/...` — 测试

  **验收标准**：
  - [ ] <!-- 具体可验证的标准 -->

  **验证命令 / 方法**：
  - `<command>`，预期：<!-- 通过条件 -->

  **依赖**：无

- [ ] 2.2 [direct] <!-- 任务描述 -->

  **需求追踪**：[R2] → [U2]
  **执行方式**：[direct]
  **涉及文件**：
  - `path/...` — 说明

  **验收标准**：
  - [ ] <!-- 具体可验证的标准 -->

  **验证命令 / 方法**：
  - `<command>`，预期：<!-- 通过条件 -->

  **依赖**：Task 2.1

<!--
规则：
1. 任务标题行只保留执行方式标签，例如 [test-first]；不要在标题行重复 [R] / [U]
2. 每个任务必须在详情中包含 **需求追踪** 字段，记录 [R<number>] → [U<number>] 映射
3. 每个任务必须包含一种执行方式：
   - [test-first]：可先写失败测试的逻辑、规则、边界处理
   - [characterization-first]：bug 修复或旧行为不清楚的改动
   - [direct]：纯样式、纯配置、纯脚手架
4. 一个任务只做一个可验证动作；如果描述里包含多个独立动作，请继续拆分
5. 每个任务必须包含涉及文件、验收标准、验证命令 / 方法、依赖
-->
